# Local deployment identity for MCP Development Bridge.
# Copy to .env and set values for this machine. Trust policy is selected
# explicitly with scripts/setup.sh --profile restricted|trusted-dev.

MCP_WORKSPACE_ROOT=/home/user/code
MCP_PUBLIC_URL=https://mcp.example.com
MCP_TUNNEL_NAME=
