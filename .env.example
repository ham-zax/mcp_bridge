# Optional Cloudflare OAuth Bridge overrides.
# Defaults are already suitable for this machine.

# Public origin used by 1MCP OAuth metadata and ChatGPT MCP connector.
TUNNEL_URL=https://mcp.hamza.my.id

# Optional named Cloudflare Tunnel. Leave empty to use cloudflared's default config.
TUNNEL_NAME=
